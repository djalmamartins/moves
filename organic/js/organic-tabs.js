(() => { const O = window.Organic = window.Organic || {}; O.Tabs = {init(root) { const tabs=[...root.querySelectorAll('[role="tab"]')]; const select=tab=>{tabs.forEach(item=>{const active=item===tab;item.setAttribute('aria-selected',String(active));item.tabIndex=active?0:-1;const panel=document.getElementById(item.getAttribute('aria-controls'));if(panel)panel.hidden=!active})}; tabs.forEach((tab,index)=>tab.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight','Home','End'].includes(event.key))return;event.preventDefault();const next=event.key==='Home'?0:event.key==='End'?tabs.length-1:(index+(event.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;tabs[next].focus();select(tabs[next])})); tabs.forEach(tab=>tab.addEventListener('click',()=>select(tab));}}; O.ready(()=>document.querySelectorAll('[data-organic-tabs]').forEach(root=>O.Tabs.init(root))); })();
